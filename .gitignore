build/
.gradle/
out/
bin/

# `gradle run` working directory: generated config, logs, and the forwarding secret.
run/

# Docker compose bind mount, same contents.
data/

logs/
*.log

# relay.py: linked backend paths, and pid/log files for servers it started.
relay-dev.json
.relay-run/

relay.toml
forwarding.secret

# Dashboard accounts: password hashes, and machine-local.
dashboard-users.json

# Storage: what happened on this node. Runtime data, not configuration.
relay.db
relay.db-shm
relay.db-wal

.idea/
*.iml
.vscode/
.DS_Store
